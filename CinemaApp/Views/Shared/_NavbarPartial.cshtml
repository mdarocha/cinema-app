@using CinemaApp.Resources

<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <a class="navbar-brand" href="@Url.Action("Index", "Home")">@Strings.AppName</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav mr-auto">
            <a class="nav-item nav-link" href="@Url.Action("Index", "Home")">@Strings.HomePage</a>
            <a class="nav-item nav-link" href="@Url.Action("Index", "Movies")">@Strings.MoviesList</a>
        </div>
        <div class="navbar-nav">
            @Html.Partial("_LoginPartial")
        </div>
    </div>
</nav>
